import React from 'react';

import { Card } from '../Common/Layout';

export function RequestStatus({ requestId }: { requestId: number }): JSX.Element {
  return <Card className="mb-4 px-6">Hey</Card>;
}
